'use strict';

var _utilHelpers = require('./util/helpers');

(0, _utilHelpers.throws)('multiple id attributes', '#one#two');
(0, _utilHelpers.throws)('unclosed pseudo element', 'button::');
(0, _utilHelpers.throws)('bad pseudo element', 'button::"after"');
(0, _utilHelpers.throws)('trailing comma', 'h1,');
(0, _utilHelpers.throws)('incorrect combinator with no left hand side', '+ h1');
(0, _utilHelpers.throws)('incorrect combinator with no right hand side', 'h1 +');
(0, _utilHelpers.throws)('unclosed attribute selector', '[name="james"][href');
(0, _utilHelpers.throws)('missing closing parenthesis in pseudo', ':not([attr="test"]:not([attr="test"])');
(0, _utilHelpers.throws)('misplaced parentheses', 'h1:not.test(h2)');